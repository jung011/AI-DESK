#!/usr/bin/env node
// AI Desk MCP server stdio entry.
import('../src/server.js').catch(err => {
  process.stderr.write(`aidesk-channel: failed to start: ${err?.message ?? err}\n`);
  process.exit(1);
});
