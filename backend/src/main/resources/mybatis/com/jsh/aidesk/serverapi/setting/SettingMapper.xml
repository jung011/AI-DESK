<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsh.aidesk.serverapi.setting.mapper.SettingMapper">

    <select id="selectValue" resultType="java.lang.String">
        SELECT setting_value
          FROM t_aidesk_setting
         WHERE account_sn = #{accountSn}
           AND setting_key = #{key}
    </select>

    <update id="upsertValue">
        INSERT INTO t_aidesk_setting (account_sn, setting_key, setting_value, updated_at)
        VALUES (#{accountSn}, #{key}, #{value}, NOW())
        ON CONFLICT (account_sn, setting_key)
        DO UPDATE SET setting_value = EXCLUDED.setting_value,
                      updated_at    = NOW()
    </update>

</mapper>
